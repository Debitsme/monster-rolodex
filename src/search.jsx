import React from "react";

function Anarkali(props) {
  return (
    <div>
      <input
        type="search"
        placeholder="Search monster"
        onChange={(e) => this.setState({ searchField: e.target.value })}
      />
      <Alop monsters={filteredMOns} />
    </div>
  );
}

export default Anarkali;
